---
 - hosts: all
   sudo: yes
   user: root
   roles:
     - Stouts.python
     - ansible-role-ruby
   vars:
     python_version: [2.7, 3.4, 3.5, ]
     python_install: [virtualenvwrapper, ]
     ruby_install_gems: [tmuxinator, ]
